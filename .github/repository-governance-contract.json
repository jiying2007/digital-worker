{
  "schema_version": 2,
  "status": "deferred-until-productionization",
  "current_stage": "iterative-development",
  "default_branch": "main",
  "stage_policy": {
    "server_side_protection_required": false,
    "blocks_real_pilot_acceptance": false,
    "repository_local_ci_still_required": true,
    "strict_enforcement_stage": "productionization"
  },
  "required": {
    "server_side_enforcement": true,
    "pull_request_required": true,
    "required_status_checks": [
      "Embedded Expert Contracts / validate",
      "Terminal Consistency / validate"
    ],
    "block_force_push": true,
    "block_branch_deletion": true
  },
  "recommended": {
    "strict_required_checks": true,
    "required_approvals": 0,
    "bounded_bypass_only": true
  },
  "notes": "During iterative development, main protection is intentionally not required and does not block real Pilot acceptance. Repository-local CI/trust and terminal-consistency gates remain active. Before productionization or equivalent multi-contributor protected operation, enable server-side enforcement for both required checks and run the governance audit in strict mode."
}

# Approved task branches pending one-shot Branch GC.
# One branch per line. `main` is forbidden by the workflow and validator.
# Eligibility is fail-closed: current tip must be an exact merged-PR head OR already contained by main.
# Empty is the healthy steady state after reviewed completed branches are removed.
codex/promote-work-run-source-set
codex/promote-work-run-source-set-fresh

# Approved merged task branches pending Branch GC.
# One branch per line. `main` is forbidden by the workflow and validator.
# Empty is the healthy steady state when no reviewed merged branch is pending deletion.
# Historical branches already deleted by prior GC runs must not remain here.
